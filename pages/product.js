import React from "react";
import styled from "styled-components";

import PageLayout from "../components/PageLayout";

const Product = () => {
  return <PageLayout>상품 목록 영역</PageLayout>;
};

export default React.memo(Product);
