import React from "react";
import styled from "styled-components";

import PageLayout from "../components/PageLayout";

const Contact = () => {
  return <PageLayout>회사 정보 / 이메일 문의</PageLayout>;
};

export default React.memo(Contact);
